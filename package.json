{
  "name": "k6-load-test",
  "version": "1.0.0",
  "description": "Load testing using k6",
  "scripts": {
    "build": "tsc && ts-node src/embed-env.ts",
    "load-test": "yarn build && k6 run dist/load-test.js"
  },
  "devDependencies": {
    "@types/k6": "^0.54.1",
    "@types/node": "^22.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
